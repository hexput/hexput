[package]
name = "hexput-check"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
rust-version.workspace = true

[lib]

[dependencies]
# AD-8 and the Spine's crate graph both say `hexput-ast` alone. The diagnostics shape reaches
# this crate through that crate's re-export; a direct `hexput-shared` edge is not in the graph.
hexput-ast = { path = "../hexput-ast" }

[lints]
workspace = true
